# Optional tools only (not used by Civil 3D / Dynamo IronPython).
# Install: double-click install_tools.bat (this folder) or install.bat (repo root), or: pip install -r requirements-tools.txt
openpyxl>=3.1.0
